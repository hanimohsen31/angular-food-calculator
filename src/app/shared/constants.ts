// the units a food item or a recipe can be measured in, the same list is
// offered wherever a unit is typed so the food table stays consistent
export const MEASURE_UNITS: string[] = ['gm', '100gm', 'item', 'kg', 'recipe'];
